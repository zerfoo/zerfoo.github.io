# Landing page refresh

The landing page now includes the existing model designer and ZIP download flow. The visual direction follows the supplied reference: Inter, quiet monochrome surfaces, a yellow-to-teal composer edge, restrained entrance motion, and light/dark themes with persisted selection. The site remains static HTML, CSS and JavaScript generated by Hugo.

Restored from `bbc7a11:content/_index.html`: Go/CLI/HTTP examples, framework features, benchmark chart and all original caveats, verified-model links, ecosystem, founder section, FAQs and footer. Updated positioning to model creation, training and execution; ownership is Sire Run, Inc. Existing historical benchmark values are retained as dated results, not new measurements. No fabricated logos or testimonials.

The designer still uses the existing API, its lifetime spending cap and local training handoff. Numeric classification remains the runnable starter; broader requests are explicitly described as design briefs. No provider or budget configuration changed.

Validation: nine API tests; existing four-width browser suite with real browser ZIP downloads; new ten-combination light/dark landing suite covering layout overflow, theme persistence, mobile menu, keyboard code tabs, chat responses, budget-exhausted fallback, copy and entrance completion. Screenshots are stored locally in `renders/`.
